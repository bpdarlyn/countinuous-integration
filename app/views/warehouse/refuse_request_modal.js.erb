$('<%= j render "form_refuse", title: "Rechazo de Entrega de Ticket" %>').on('hidden.bs.modal', function (e) {
  $(this).remove();
  $('.modal-backdrop').remove();
}).modal({backdrop: 'static', keyboard: false});
